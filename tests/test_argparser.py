import os
from subprocess import Popen, PIPE


def test_argsparsing():
    # path = os.path.abspath(__file__ + "/../../source/main.py")

    # p = Popen(
    #     ['python', path, '123245/fwebrg/vfeve'],
    #     stdin=PIPE,
    #     stdout=PIPE,
    #     stderr=PIPE
    # )
    # output, err = p.communicate()
    # rc = p.returncode
    # assert '123245/fwebrg/vfeve' in output.decode("utf-8")
    # assert rc == 0
    pass
